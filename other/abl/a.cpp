#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main(){
    int K;
    cin >> K;
    string ans;
    rep(i,K) ans += "ACL";
    cout << ans << endl;
}