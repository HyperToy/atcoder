#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main(){
    int N, D;
    cin >> N >> D;
    vector<int> p(N), q(N);
    rep(i,N) cin >> p[i];
    rep(i,N) cin >> q[i];

    
} 